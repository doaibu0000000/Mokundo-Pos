# Project Rules

## Automatic Git Push
Always run `git add`, `git commit`, and `git push` automatically to push to GitHub every time you make a change to the code in this workspace. Do not wait for the user to ask for it.
